
for (let i = 0; i < 1; i++) {
    let row = "";
    for (let j = 0; j < 1; j++) {
      row += " ***** ";
    }
    console.log(row);
  }
  
  let row = "";
  for (let j = 0; j < 1; j++) {
    row += "*     *";
  }
  console.log(row);
  
  for (let z = 0; z < 1; z++) {
    row = "* * * *";
  }
  console.log(row);
  
  for (let z = 0; z < 1; z++) {
    row = "*  *  *";
  }
  console.log(row);
  
  for (let z = 0; z < 1; z++) {
    row = "* *** *";
  }
  console.log(row);
  
  for (let z = 0; z < 1; z++) {
    row = "*     *";
  }
  console.log(row);
  
  for (let z = 0; z < 1; z++) {
    row = " ***** ";
  }
  console.log(row);